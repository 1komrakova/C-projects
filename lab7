#include <stdio.h>
#include <string.h>

struct human {
	char name[50];
	char lastname[50];
	int year;
};
	
int main() {
	struct human a[4];
	struct human b[4];
	int i, j;
	for(i = 0; i < 4; i++){
	printf("enter name, lastname and the year:\n");
	scanf("%s50", a[i].name); 
	scanf("%s50", a[i].lastname);
	scanf("%d", &a[i].year);
	}
	struct human tmp[1];
	for(i = 0; i < 3; i++){
		for(j = 0; j < 3; j++){
			if(a[j].year > a[j+1].year){
				tmp[i] = a[j];
				a[j] = a[j+1];
				a[j+1] = tmp[i];
			}
		}
		
	}
	for(i = 0; i < 4; i++){
	b[i] = a[i];
	}
	
	for(i = 0; i <4; i++){
		printf("%s %s %d\n", b[i].name, b[i].lastname, b[i].year);
	}
	return 0;
}
	
